# MoA Drug Prediction — Python Dependencies
# ==========================================
# Install all dependencies with:
#   pip install -r requirements.txt
#
# For GPU support (CUDA), install PyTorch separately:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# ── Core Deep Learning ────────────────────────────────────────────────────────
torch>=2.0.0                # PyTorch — the main neural network framework
torchvision>=0.15.0         # Image utilities (needed by some torch dependencies)

# ── Data Processing & Machine Learning ───────────────────────────────────────
numpy>=1.24.0               # Fast numerical array operations
pandas>=2.0.0               # DataFrame operations for loading and processing CSV files
scikit-learn>=1.3.0         # Preprocessing (StandardScaler), metrics (AUROC, F1), kNN imputation

# ── Dashboard & Visualization ─────────────────────────────────────────────────
dash>=2.14.0                # Dash web framework — the main dashboard library (Plotly ecosystem)
plotly>=5.17.0              # Interactive charts (used by Dash)
dash-bootstrap-components>=1.5.0  # Bootstrap styling components for Dash

# ── Explainability ────────────────────────────────────────────────────────────
shap>=0.43.0                # SHAP values for model interpretability

# ── Cheminformatics (for Phase 2+) ────────────────────────────────────────────
# rdkit                     # Chemical fingerprints (ECFP4) — install separately:
#                           #   conda install -c conda-forge rdkit
#                           # or: pip install rdkit-pypi

# ── Graph Neural Networks (for Phase 3+) ──────────────────────────────────────
# torch-geometric           # PyTorch Geometric (GAT model) — install via:
#                           #   pip install torch-geometric
# dgl                       # Deep Graph Library (alternative GNN framework)

# ── API Backend (for Phase 4) ─────────────────────────────────────────────────
# fastapi>=0.104.0          # REST API framework
# uvicorn>=0.24.0           # ASGI server for FastAPI

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.66.0                # Progress bars for training loops and data loading
matplotlib>=3.7.0           # Static plotting (used for saving figures to outputs/figures/)
seaborn>=0.13.0             # Statistical visualization on top of matplotlib
kaggle>=1.5.16              # Kaggle CLI for downloading the competition dataset

# ── Development Tools ─────────────────────────────────────────────────────────
jupyter>=1.0.0              # Jupyter notebooks (for exploration in notebooks/ folder)
ipykernel>=6.25.0           # Jupyter kernel for VS Code integration
